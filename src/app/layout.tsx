import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ImageProof Studio - Professional Image Verification",
  description:
    "Professional image verification and annotation tool with ProofSnap evidence integration. Analyze suspicious images and generate comprehensive reports.",
  keywords: [
    "image verification",
    "deepfake detection",
    "AI image analysis",
    "ProofSnap",
    "forensic analysis",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
